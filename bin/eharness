#!/usr/bin/env python3
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))
if len(sys.argv) > 1 and sys.argv[1] == "wire":
    from wire.cli import main
    main(sys.argv[2:])
else:
    from cctv.cli import main
    main()
